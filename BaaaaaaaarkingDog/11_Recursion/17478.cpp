#include <bits/stdc++.h>
using namespace std;

int n;
string str; // ❓ 여러줄 문자열 파싱 어떻게 하지? 더군다나 "가 들어간 문자열은?

void func(int n)
{
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;

    func(n);
}
