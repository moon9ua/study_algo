#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n = 3;
    while (n--)
    {
        list<char> lst(3);
        // free(a);
        vector<char> v;
    }
    while (1)
    {
    }
}
