#include <bits/stdc++.h>
using namespace std;

int r, c;
string board[1002];
int vis[1002][1002];
int dx[4] = {1, 0, -1, 0};
int dy[4] = {0, 1, 0, -1};

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> r >> c;
    for (int i = 0; i < r; i++)
    {
        cin >> board[i];
    }
}

// 너무 졸려서 내일...